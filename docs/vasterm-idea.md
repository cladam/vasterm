## Original idea for vasterm

First, the name is a portmanteau of "vast" and "terminal". Vast as in a large, expandable canvas, and term as in a terminal.

The idea is to combine the principles of a terminal multiplexer (like tmux), a tiling window manager, and a zoomable user interface (ZUI).

Let's see how it goes...